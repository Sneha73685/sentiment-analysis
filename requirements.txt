# Pinned to exact versions verified working in the bert310 conda environment
# (Python 3.10) as of 2026-08-19. Exact pins (not ranges) intentionally, to
# remove the environment drift previously found between this file and the
# working environment.
torch==2.10.0
transformers==4.41.2
datasets==2.21.0
evaluate==0.4.6
accelerate==0.34.2
pandas==2.3.3
scikit-learn==1.7.2
scipy==1.15.3
nltk==3.9.2
joblib==1.5.3
numpy==2.2.6
pyspellchecker==0.9.0
matplotlib==3.10.9
seaborn==0.13.2

# Test-only dependency (not required by any src/ or experiments/ runtime
# code) -- needed to run the tests/ suite added in the Phase D repository
# hygiene work.
pytest==9.1.1
